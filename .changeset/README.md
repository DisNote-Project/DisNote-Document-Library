# Changesets

This folder holds [changesets](https://github.com/changesets/changesets). Every
change to a published package needs a changeset describing the version bump and
a human-readable summary. Run `npx changeset` to create one.
