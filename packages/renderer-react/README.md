# @disnote/renderer-react

Lightweight React read renderer with SSR, resolvers, themes and custom block
renderers. It does not load the editor.

```tsx
import { DocumentRenderer } from "@disnote/renderer-react";
```
