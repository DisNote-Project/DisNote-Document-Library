# @disnote/assets

Upload validation, magic-byte MIME detection and a reference uploader.

```ts
import { InMemoryAssetUploader } from "@disnote/assets";

const uploader = new InMemoryAssetUploader();
```

See the repository `docs/FEATURES_AND_USAGE.md` for configuration and security
boundaries.
