# @disnote/renderer-html

Safe deterministic HTML/SSR renderer with headings, assets and warning
projections. It has no React, DOM or editor dependency.

```ts
import { renderDocumentToHtml } from "@disnote/renderer-html";
```
