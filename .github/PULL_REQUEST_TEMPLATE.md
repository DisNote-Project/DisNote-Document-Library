## Summary

<!-- What does this change and why -->

## Checklist

- [ ] `npm run typecheck` passes
- [ ] `npm test` passes
- [ ] Changeset added (`npx changeset`) if a published package changed
- [ ] No vendor type leaked into `document-core`
- [ ] Unknown blocks still preserved
- [ ] Fixtures / round-trip tests updated if the schema changed
